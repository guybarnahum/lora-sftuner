# -----------------------------------------------------------------------------
# Environment Variables & Secrets for lora-sftuner
#
# This file contains secrets and machine-specific environment settings.
# Copy this to .env and fill in your values.
# -----------------------------------------------------------------------------

# --- REQUIRED SECRETS ---
# Your Hugging Face Hub token (for accessing gated models like Gemma)
HUGGINGFACE_HUB_TOKEN=hf_...

# Your Twitter Bearer Token for using the API via `lora-sftuner sync`
TWITTER_BEARER_TOKEN=YOUR_TOKEN_HERE


# --- USER-SPECIFIC SETTINGS ---
# Your Twitter handle (without the '@') for the sync command
TWITTER_USERNAME=your_handle


# --- GPU / RUNTIME ENVIRONMENT ---
# These are optional settings to control the execution environment.

# Select which GPU to use (e.g., 0 for the first one)
# CUDA_VISIBLE_DEVICES=0

# Move the Hugging Face cache to a different directory
# HF_HOME=~/.cache/huggingface

# Silence the bitsandbytes welcome banner
BITSANDBYTES_NOWELCOME=1

# Advanced PyTorch memory management setting
# PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:128

